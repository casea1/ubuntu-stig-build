---
- name: Restart chrony
  ansible.builtin.systemd:
    name: chrony
    state: restarted
  failed_when: false

- name: Restart ssh
  ansible.builtin.systemd:
    name: ssh
    state: restarted
  failed_when: false
