---
- name: restart freshclam
  ansible.builtin.systemd:
    name: clamav-freshclam
    state: restarted

- name: restart clamav-daemon
  ansible.builtin.systemd:
    name: clamav-daemon
    state: restarted
  failed_when: false      # freshclam may not have primed a database yet on first run
