# Managed by ansible (ai_compose) -- values come from site.yml (out-of-band).
# Root-only. Referenced by docker-compose.yaml via ${VAR}.
POSTGRES_PASSWORD={{ ai_pgvector_password }}
SYSTEM2_IP={{ ai_system2_ip }}
